# Ask Mode AGENTS.md

- `src/shared/`: Canonical source for components, hooks, and utilities shared between Electron and Web remote.
- `src/main/`: Core Electron backend, menu, environment management.
- `src/renderer/`: UI implementation, state (Zustand), and TanStack Query logic.
- Documentation: Check `docs/` for specific environment setup (e.g., NIX_INSTALL, ENV_SETTINGS).
